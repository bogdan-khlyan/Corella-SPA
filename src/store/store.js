import Vuex from 'vuex'
import userInstance from '@/app/userInstance/user-instance.store'

export default new Vuex.Store({
    state: {
    },
    mutations: {
    },
    modules: {
        userInstance
    }
})
